# 广告订单 OA 全栈完善开发计划

## 1. 目标与基线

### 1.1 目标

以 `pics/demo3.html` 为当前最新的交互、业务流程和视觉参考，在现有 SvelteKit + SQLite 全栈项目上继续开发一个可持久化、可登录、可按身份授权的广告订单 OA：

- 用户可以登录后进入订单工作台；
- 订单列表、详情、新增、编辑、筛选、统计和导出与 demo3 的操作路径保持一致；
- 产品报价库、厂商成本库能够真正持久化、配置和参与订单录入；
- 员工报销、财务审核、打回重传、待付款、已报销和单据归档形成完整闭环；
- 附件、导出、状态流转和权限由服务端控制，而不是依赖浏览器内存或前端身份切换；
- 视觉上采用 demo3 的深色侧栏、蓝色主操作、浅灰背景、白色面板、状态标签和响应式表格设计。

### 1.2 当前开发分支

已从 `main` 创建并切换到：

```text
feature/demo-fullstack-auth-plan
```

本文件是本分支的开发计划，不在本阶段直接实现功能。

### 1.3 当前技术基线

- SvelteKit 2.20 + Svelte 5 + TypeScript；
- `@sveltejs/adapter-node`，Node 20；
- SQLite + `better-sqlite3`，数据库迁移集中在 `src/lib/server/order-db.ts`；
- `xlsx` 用于 Excel 解析和服务端导出；
- `lucide-svelte` 图标；
- 根页面目前是单页工作台，通过 `view` 状态在订单、录入、报销和报价成本库之间切换；
- 已有真实订单、项目、客户、报价成本、报销、订单附件和发票附件 API；
- 当前数据库 schema 版本为 `11`。

---

## 2. 调研结论

### 2.1 最新 demo 的判定

`pics/demo3.html` 是仓库中最新的 HTML demo：

- 约 4,800 行，功能和样式覆盖最完整；
- 在提交 `c8913ff feat: align order workflow with latest demo` 中新增；
- 相比 `demo.html`、`demo2.html`，增加了报价/成本库配置、导出模板、Excel 生成、报销付款工作台、单据存档、角色适配和多轮响应式修正；
- `pics/广告订单 OA · 可操作 Demo.html` 是较早的可操作版本，不作为本轮主要基准。

### 2.2 demo3 的产品结构

#### 导航与身份

- 左侧深色导航：订单列表、我的报销/报销管理、报价库与成本库；
- 顶部提供“当前身份”切换，demo 中包含员工和财务管理员；
- 员工只能看到自己的报销；财务进入审核、付款和单据归档工作台；
- 正式项目不能照搬前端身份切换，身份应由登录会话和服务端权限决定。

#### 订单列表

- 日期范围、客户、设计师、结款状态、关键词筛选；
- 日期新到旧、旧到新、销售额高到低排序；
- 关键词覆盖订单号、产品和联系人；
- 统计卡片与筛选结果同步：销售总额、制作成本、员工垫付、预计毛利、未结款金额；
- 可配置显示部门、联系人、设计师、制作状态、结款状态、成本、垫付和毛利字段；
- 点击表格行进入只读详情，详情页再进入编辑；
- 支持导出结算单和自选字段明细。

#### 订单录入

- 基础信息：客户、客户部门、联系人/下单人、下单日期、交货日期、指定设计师、开单员、订单状态；
- 产品明细：可添加/删除多行，选择报价库产品后自动带出单位、单价和制作要求；
- 固定厂商成本：可添加/删除多行，选择成本项目和供应商后带出单位、成本价；同一项目可有多个供应商报价；
- 员工垫付：可添加/删除，填写物品、金额、日期和发票；
- 数量、单价、小计和订单销售/成本/毛利联动；
- 产品为空时禁止保存；其它附加信息允许留空；
- 新增和编辑共用一套表单。

#### 报销流程

demo3 的目标状态流转为：

```text
员工提交 → 待审核 → 待打款 → 已报销
                    ↘ 已打回 → 员工重新上传 → 待审核
```

员工端：

- 新增订单报销或内务报销；
- 按日期、类型、状态筛选；
- 只查看本人记录；
- 显示打回原因，支持重新上传发票；
- 查看报销单据和附件。

财务端：

- 按日期、员工、类型、状态筛选；
- 选择员工后进入审核队列；
- 单条或批量通过、打回；打回必须填写员工可见原因；
- 按员工汇总待打款单数和金额；
- 单员工或批量确认已打款；
- 生成、查看、归档报销单据；
- 导出报销明细和待付款汇总。

#### 报价库与成本库

demo3 将基础资料分为两个业务视图：

- 客户报价库：按公司维护报价文件及产品明细；
- 厂商成本库：按供应商维护成本文件及成本明细；
- 支持新增公司/厂商、上传或沿用其它库文件、查看明细、关键词搜索；
- 配置后的数据要直接参与订单产品和成本行的联想、单位、供应商、价格和制作要求回填；
- 原型用浏览器变量保存，正式项目必须服务端持久化。

#### 导出与附件

- 结算单模板：标题、甲乙方、合同编号、跟进人、按产品展开、分类、小计、总计和签署栏；
- 自选字段明细：可选择订单字段，可按产品逐行展开；
- demo 支持浏览器生成 XLSX、复制表格、下载兜底和 AI 通道占位；本项目优先保留可靠的服务端导出和复制能力，不依赖 `catscoArtifactHost`；
- 附件支持发票/图片/PDF预览和下载，正式实现必须校验访问权限。

### 2.3 当前项目已有能力

当前项目已不是空壳，已有以下可复用基础：

- 订单、客户、项目、报价成本库、报销、附件和 Excel 导出 API；
- 订单总览、详情、新增/编辑和删除；
- 订单筛选、排序、金额汇总、字段显示控制；
- 产品、固定成本、员工垫付动态行和报价/成本联想；
- SQLite 金额按分存储，基础输入校验和事务处理；
- 真实附件落盘、魔数校验、大小限制、附件查看接口；
- 报销待核验、打回、待报销、已报销状态及批量处理；
- 报销发票重新上传会从已打回回到待核验；
- 待付款员工汇总和报销单据编号；
- 报价/成本 Excel 导入及订单结算明细导出；
- `docs/demo3-review.md` 已记录过一次 demo 与项目差异。

### 2.4 当前项目的主要问题

1. **没有登录和服务端身份**：`hooks.server.ts` 只设置安全响应头，所有 API 当前无需登录；侧边栏的员工/财务选择只是客户端状态。
2. **填写人依赖 localStorage**：`creatorName` 可被任意修改，不能作为审计身份；报销查询也依赖前端传入的名字。
3. **页面过度集中**：`orderDeskState.svelte.ts` 约 1,300 行，承担数据、筛选、表单、上传、导出和报销工作流；`app.css` 和多个页面组件也有较多混合逻辑。
4. **URL 不表达当前页面**：刷新或分享无法稳定回到订单详情、录入或报销筛选状态；当前页面主要依赖根组件内的 `view` 状态。
5. **报价/成本库仍是统一展示**：已有导入和匹配，但没有 demo3 中按客户/厂商管理、替换来源、查看明细的完整交互。
6. **数据模型仍有原型痕迹**：产品和成本主要保存在 `orders_simple.products_json` / `costs_json`；基础资料的来源、归属和版本关系不够明确；缺少完整操作审计。
7. **状态命名不完全一致**：当前使用“待核验/待报销”，demo 使用“待审核/待打款”；需要统一内部状态编码和显示文案，避免迁移后逻辑分叉。
8. **权限和附件保护不足**：订单、报销和附件接口没有基于当前用户检查可见范围；下载接口只校验附件是否存在。
9. **错误处理粒度不足**：`action()` 将大量业务错误统一返回 400；缺少统一的 401、403、404、409 和字段级校验结构。
10. **导出能力部分已具备但需对齐**：当前已有服务端 detail/settlement 导出，但还需补齐 demo3 的字段、分类、模板信息、客户跟进人及前端导出状态反馈。

---

## 3. 产品与权限方案

### 3.1 最小身份模型

先实现简单、可维护的三种角色，不引入复杂组织架构：

| 角色 | 典型用户 | 主要能力 |
|---|---|---|
| `employee` 员工 | 设计师、业务员 | 登录、查看订单、创建/编辑允许范围内的订单、提交本人报销、查看本人状态、补传附件 |
| `finance` 财务 | 财务人员 | 查看全量订单和报销、审核/打回、批量处理、确认付款、生成单据、导出财务数据 |
| `admin` 管理员 | 系统管理员 | 拥有财务权限，管理用户、角色、客户项目、报价库/成本库及系统配置 |

权限必须由服务端判定。demo 的“当前身份”切换只作为视觉参考；正式页面显示当前登录用户，不允许普通用户通过下拉框提升为财务。

### 3.2 登录与会话

采用满足“最简单身份系统”且不引入额外认证服务的方案：

- 新增 `/login` 页面，用户名 + 密码登录；
- 新增 `users` 表：`id`、`username`、`display_name`、`password_hash`、`role`、`active`、`created_at`、`updated_at`；
- 使用 Node `crypto.scrypt` 哈希密码，不明文存储；
- 新增 `sessions` 表：`id`、`user_id`、`token_hash`、`expires_at`、`created_at`、`last_seen_at`；
- 登录成功后只把随机 session token 的原值写入 HttpOnly、SameSite=Lax Cookie，数据库保存 hash；
- Cookie 在生产环境开启 `Secure`，会话默认 7 天，可退出并主动失效；
- 新增 `/api/auth/login`、`/api/auth/logout`、`/api/auth/me`；
- `hooks.server.ts` 解析会话并写入 `event.locals.user`；
- 页面访问未登录时跳转 `/login`，API 未登录返回 401；
- 页面和 API 都通过统一 `requireUser` / `requireRole` 工具保护；
- 首次部署通过环境变量或一次性初始化脚本创建管理员，禁止把生产默认密码硬编码进代码；
- 为登录失败、过期 session、禁用用户提供明确错误，不泄漏“用户名是否存在”。

### 3.3 与现有数据的兼容

- 旧订单的 `created_by` 和旧报销的 `employee` 暂时保留为显示快照；
- 增加 `created_by_user_id`、`employee_user_id` 等关联字段，已有历史数据不能匹配用户时保留原文本并标记为“历史数据”；
- 移除用 localStorage 决定权限的逻辑；用户姓名默认来自会话，但订单允许保留业务上的“开单员”显示字段；
- 访问范围建议：所有已登录内部用户可查看订单列表，员工只能修改自己创建且处于可编辑状态的订单；删除、改结款状态、导出全量数据由财务/管理员控制；具体权限在实现前以产品验收为准。

---

## 4. 目标信息架构与视觉规范

### 4.1 页面结构

逐步从单一根页面拆为可刷新、可分享的 URL：

```text
/login
/
/orders
/orders/new
/orders/[id]
/orders/[id]/edit
/reimbursements
/catalog
/admin/users       # 第二阶段，可选
```

`/` 重定向到 `/orders`。保留现有组件并逐步迁移，不一次性重写所有订单逻辑。

### 4.2 视觉基线

以 demo3 的最终视觉覆盖为准，建立统一 CSS token，不复制 demo3 多层覆盖的单文件 CSS：

```css
--navy: #17243d;
--navy-2: #24385d;
--blue: #315bdc;
--blue-strong: #2349bd;
--teal: #0f6d57;
--teal-soft: #e9f6f1;
--amber: #865700;
--amber-soft: #fff4d6;
--red: #b54949;
--red-soft: #fdecec;
--ink: #1b2738;
--muted: #5f6e82;
--line: #dfe5ee;
--line-soft: #edf1f5;
--bg: #f3f5f8;
```

统一要求：

- 深色固定侧栏、品牌区和角色/账号信息；
- 主区浅灰背景，页面使用白色 panel/card；
- 主按钮使用蓝色，成功状态使用绿色，提醒使用琥珀色，危险操作使用红色；
- 表格表头浅灰、行 hover、金额右对齐、状态使用 tag；
- 详情和编辑明确区分“查看模式”和“编辑模式”；
- 表单控件高度、圆角、focus ring、空状态、loading 和 toast 统一；
- 低于 1000px 开始压缩布局，低于 700px 允许表格横向滚动并将筛选收进抽屉；
- 无障碍：按钮有 aria-label，表格行可键盘进入详情，弹窗支持 Escape 和焦点处理。

### 4.3 前端拆分建议

将当前大状态文件拆成以下边界，先保持 Svelte 5 `$state` 用法，避免引入不必要的状态库：

- `src/lib/stores/session.svelte.ts`：当前用户、登录态、登出；
- `src/lib/features/orders/orderState.svelte.ts`：订单列表、筛选、统计、选择和导出参数；
- `src/lib/features/orders/orderFormState.svelte.ts`：订单表单、动态明细和校验；
- `src/lib/features/reimbursements/reimbursementState.svelte.ts`：员工/财务列表、选择和工作流动作；
- `src/lib/features/catalog/catalogState.svelte.ts`：客户报价库、厂商成本库、导入和查看器；
- `src/lib/components/` 下拆分 `FilterBar`、`MetricCards`、`DataTable`、`StatusTag`、`Drawer`、`ConfirmDialog`、`FilePreview`、`DynamicLineItems`；
- `src/lib/server/` 拆出 `auth.ts`、`permissions.ts`、`audit.ts`、`order-repository.ts`、`catalog-repository.ts`、`reimbursement-repository.ts`，逐步缩小 `order-db.ts`。

---

## 5. 数据库与后端设计

### 5.1 迁移版本规划

当前 schema 为 11，后续使用显式递增迁移，禁止在生产数据上直接 drop/recreate：

- **v12：身份与会话**：`users`、`sessions`、必要的用户关联字段；初始化管理员和开发演示用户；
- **v13：统一业务状态和审计**：报销状态采用稳定英文 code，增加 `audit_logs`，迁移旧状态：`待核验 → pending_review`、`待报销 → pending_payment`、`已报销 → paid`、`已打回 → rejected`；前端继续显示 demo 语义“待审核/待打款”；
- **v14：基础资料归属**：新增 `catalog_sources`（类型 quote/cost、公司/厂商名称、来源文件、来源方式、启用状态、维护人），`catalog_items` 关联 source；兼容已有 `customer_name`、`source_file` 等字段；
- **v15：订单明细规范化（可分阶段）**：将 `products_json`、`costs_json` 迁移到 `order_products`、`order_costs`，保留快照字段，确保历史订单不受报价库变更影响；
- **v16：附件与业务身份**：附件增加上传人、分类、可见范围/关联报销人等字段，便于权限审计和清理。

每次迁移必须：

- 在事务中执行；
- 可从现有 `data/oa.db` 备份后回滚；
- 提供旧数据数量和失败记录检查；
- 在测试数据库跑完整升级，再运行 `npm run check` 和手工验收。

### 5.2 报销领域模型

推荐内部使用枚举 code，显示层使用中文：

```text
pending_review  待审核
rejected        已打回
pending_payment 待打款
paid            已报销
```

允许的状态转移：

```text
pending_review -> pending_payment
pending_review -> rejected
rejected -> pending_review       # 员工补充附件/资料
pending_payment -> paid
```

所有状态变更记录：操作者、原状态、新状态、打回原因、时间、关联附件。单据只能由 `pending_payment` 或 `paid` 生成，单号唯一且幂等。

### 5.3 API 规范

保留现有 API 路径，逐步增加鉴权和统一响应：

- `GET/POST /api/orders`；`PATCH/DELETE /api/orders/[id]`；
- `GET /api/orders/export`；
- `GET/POST /api/projects`、`GET /api/customers`；
- `GET/POST /api/catalog`，后续增加 source、明细和导入预览接口；
- `GET/POST/PATCH/DELETE /api/reimbursements` 及批量、单据、附件接口；
- 新增 `/api/auth/*`、`/api/users/*`（管理员阶段）；
- 所有写接口校验当前用户、角色、记录归属和状态转移；
- 统一错误结构：`{ error: { code, message, fields? } }`，分别使用 400/401/403/404/409/413/500；
- 列表接口支持服务端筛选、分页、排序，第一阶段可以兼容现有前端内存筛选，第二阶段迁移到服务端查询；
- 所有金额由服务端按分计算，客户端只用于即时预览；
- 上传接口复用现有魔数、大小、扩展名校验，但新增登录和业务记录权限校验。

### 5.4 附件安全

- 订单附件只能由有权查看该订单的用户下载；
- 发票附件只能由对应员工本人、财务或管理员查看；
- 响应使用安全的 `Content-Disposition` 文件名，禁止路径穿越；
- 记录上传人、类型、大小、上传时间；
- 继续限制图片/PDF和单文件 10MB，后续增加总量和数量限制；
- 删除订单/报销时在事务后清理物理文件，并记录清理失败日志；
- 不向未登录用户暴露附件 URL。

---

## 6. 分阶段开发计划

### 阶段 0：基线与登录骨架

**目标**：在不破坏现有订单功能的情况下，让系统先具备可用的登录和服务端身份。

任务：

1. 增加 `event.locals.user` 类型和 auth/session 服务；
2. 实现 users/sessions 表及 v12 迁移；
3. 实现登录页、登录/退出/当前用户接口；
4. 保护根页面和现有 API；
5. 将侧栏身份切换改为当前用户信息，去掉 localStorage 对权限的影响；
6. 提供开发环境初始化用户说明，不提交真实密码；
7. 为订单、报销、附件 API 加上最小权限检查。

验收：

- 未登录访问工作台跳转 `/login`；
- 错误密码不能登录，退出后旧 session 立即失效；
- 普通员工不能通过修改请求参数读取别人的报销；
- 财务账号能访问审核队列，普通员工不能调用审核/付款接口；
- 现有数据库升级后历史订单仍可读取。

### 阶段 1：路由、布局和 demo3 视觉对齐

**目标**：先统一用户看到的框架和导航体验。

任务：

1. 将根页面重定向到订单列表；
2. 增加订单、详情、录入、报销、基础资料的 URL 路由或可恢复的 query 状态；
3. 重构 CSS token、按钮、panel、table、tag、drawer、modal、toast 和响应式规则；
4. 深色侧栏显示品牌、当前用户、角色和退出；
5. 删除 demo3 中不可复用的 artifact bridge、内存 seed 和前端角色伪装；
6. 统一 loading、空状态、错误提示、确认弹窗和 Escape 行为。

验收：

- 刷新详情/录入/报销页面不会回到错误视图；
- 视觉上符合 demo3 的颜色、间距、表格和按钮层级；
- 700px 以下仍可完成核心操作，表格不发生严重挤压。

### 阶段 2：订单列表、详情和录入闭环

**目标**：达到 demo3 的订单主流程，并改善当前项目的可维护性。

任务：

1. 将筛选、排序、统计和字段选择拆成独立组件；
2. 对齐 demo3 的日期、客户、设计师、结款、关键词和销售额排序；
3. 保留当前项目已有的项目、负责人、录入人、交货日期等增强字段；
4. 订单详情明确显示基础信息、产品、固定成本、垫付、附件和毛利；
5. 订单编辑复用新建表单，避免从详情进入时丢失状态；
6. 产品行：报价库联想、单位/单价/制作要求回填、实时小计；
7. 成本行：成本库联想、厂商选择、单位/成本价回填、实时小计；
8. 垫付行：员工、物品、金额、日期、发票附件；
9. 统一金额、数量、交货日期、至少一项产品等前后端校验；
10. 根据权限控制新建、编辑、删除和结款状态修改。

验收：

- 新增订单至少包含一项产品才能保存；
- 报价库/成本库选中后字段自动带出，手动修改后不会错误保留旧关联；
- 订单销售额、成本、垫付、预计毛利与数据库结果一致；
- 刷新后数据、附件和报销关联不丢失；
- 关键操作有成功/失败反馈，重复提交不会生成重复订单。

### 阶段 3：报价库与成本库完整持久化

**目标**：把当前“导入并统一展示”提升为 demo3 的两个可维护基础资料域。

任务：

1. 引入 `catalog_sources` 和 source-item 关系；
2. 客户报价库按公司展示项目数、来源文件和配置状态；
3. 厂商成本库按供应商展示项目数、来源文件和配置状态；
4. 支持新增公司/厂商、配置新文件、沿用已有来源；
5. 导入前解析预览，显示列映射、有效行数、被忽略行和错误原因；
6. 支持 CSV/XLS/XLSX，保留原始行数据和制作要求/备注；
7. 增加全屏明细查看器和关键词过滤；
8. 只有财务/管理员可以写入或替换基础资料，员工只读；
9. 订单录入按客户/项目/厂商过滤匹配，避免跨客户错误报价。

验收：

- 新建公司/厂商后可以配置库，不刷新页面也能出现在订单表单；
- 同一成本项目的不同供应商可分别查看和选择；
- 替换报价库不会改变历史订单中的已保存单价；
- 导入结果可追溯到文件、sheet、维护人和时间。

### 阶段 4：报销状态机、财务工作台和单据

**目标**：完整实现 demo3 的员工/财务双视图，但权限来自后端身份。

任务：

1. 完成 v13 状态迁移和显示文案统一；
2. 员工端只显示本人的订单报销和内务报销；
3. 新建报销支持关联订单、日期、金额、备注和发票；
4. 财务端先选员工，再查看该员工审核队列；
5. 支持单条/批量审核通过和打回，打回原因必填；
6. 员工看到打回原因后可重新上传，状态自动回到待审核；
7. 生成报销单据必须幂等，支持查看、归档和下载；
8. 按员工汇总待打款记录，支持核对明细、单员工付款和批量付款；
9. 付款和状态变更写审计日志；
10. 对订单中的员工垫付和独立内务报销使用同一套状态机。

验收：

- 员工无法查看其他员工的金额、发票和打回原因以外的非本人信息；
- 财务通过后记录进入待打款，确认付款后进入已报销；
- 打回必须有原因，重传后回到待审核；
- 批量操作遇到非法状态时整体事务回滚，不产生半成功；
- 同一条报销重复生成单据不会重复编号。

### 阶段 5：导出、附件和历史可追溯

**目标**：让 demo3 的导出和文件体验达到生产可用，而不是只做视觉模拟。

任务：

1. 对齐结算单字段：标题、合同编号、甲乙方、跟进人、联系电话、备注订单号；
2. 结算单按产品和业务分类输出，每类小计、总计和签署栏；
3. 自选字段明细支持逐产品展开、金额合计和当前筛选条件；
4. 报销明细、待付款汇总、单据分别支持导出；
5. 导出参数在服务端重新查询和鉴权，不能信任前端传入的金额；
6. 附件列表支持图片/PDF预览、下载和文件信息；
7. 下载失败、文件缺失、权限不足给出明确反馈；
8. 对导出、上传、下载、单据生成写入审计日志。

验收：

- 当前筛选结果与导出行数、金额一致；
- 结算单可以被 Excel 正常打开，中文、金额、合计和签署栏正确；
- 无权限用户无法通过改 URL 下载附件或导出其它人的报销；
- 服务器重启后订单、附件、单据仍可用。

### 阶段 6：测试、部署和收尾

**目标**：从“可演示”变成“可交付”。

任务：

1. 增加数据库迁移测试和核心 repository 测试；
2. 增加 auth/permission API 测试；
3. 增加订单金额、状态流转、批量事务和导出测试；
4. 增加登录、订单录入、报销打回重传、财务付款的浏览器验收脚本；
5. 运行 `npm run check`、`npm run build`，检查生产 adapter-node 启动；
6. 完善 `.env.example`：数据库路径、session 配置、初始化管理员配置、附件目录；
7. 写明 SQLite 备份、附件目录备份、迁移和恢复流程；
8. 删除临时调试代码、重复 CSS、未使用 import 和原型 seed 逻辑；
9. 更新 README，说明开发账号、角色、数据初始化和部署。

---

## 7. 测试与验收清单

### 7.1 登录与权限

- [ ] 未登录页面和 API 均被拦截；
- [ ] 登录、退出、过期 session、禁用用户行为正确；
- [ ] 员工/财务/管理员权限边界正确；
- [ ] 不能靠请求体中的 `actor`、`employee` 或 `role` 冒充其它用户；
- [ ] Cookie 使用 HttpOnly、SameSite，生产环境 Secure。

### 7.2 订单

- [ ] 筛选条件同步影响列表和五项统计；
- [ ] 产品、成本、垫付动态行增删和金额计算正确；
- [ ] 订单详情、编辑、取消、删除权限正确；
- [ ] 订单与项目/客户外键有效；
- [ ] 重复提交和非法金额不会写入脏数据；
- [ ] 历史订单保存的是当时的名称、单位、单价和制作要求快照。

### 7.3 报销

- [ ] 订单报销和内务报销均能创建；
- [ ] 员工只能查看自己的记录；
- [ ] 待审核 → 待打款 → 已报销流程正确；
- [ ] 打回原因必填，重传后重新进入待审核；
- [ ] 批量操作事务一致；
- [ ] 单据编号唯一、幂等、可查看和导出。

### 7.4 基础资料、附件与导出

- [ ] 报价库按客户、成本库按厂商分别维护；
- [ ] Excel/CSV 导入有预览和错误反馈；
- [ ] 附件真实存储、权限校验、文件类型和大小校验正确；
- [ ] 导出结果与服务端筛选和金额一致；
- [ ] 图片/PDF能预览，文件丢失时有可理解错误。

### 7.5 视觉和交互

- [ ] 颜色、按钮层级、panel、table、tag 与 demo3 对齐；
- [ ] loading、toast、空状态、确认弹窗完整；
- [ ] Escape 可关闭抽屉/弹窗，焦点不丢失；
- [ ] 桌面端和移动端均可完成订单和报销核心流程；
- [ ] 不直接复制 demo3 的字符串拼接 HTML、内存数据或 artifact bridge。

---

## 8. 风险、取舍与优先级

### 高优先级

1. 登录、session、服务端权限和附件保护；
2. 订单主流程不能回归；
3. 报销状态机、打回重传和财务付款闭环；
4. 数据迁移可回滚，历史数据不丢失。

### 中优先级

1. 报价库/成本库按客户和厂商拆分管理；
2. demo3 结算单模板和字段导出；
3. URL 路由化和前端状态拆分；
4. 审计日志和管理员用户管理页。

### 可后置

1. 复杂组织架构、部门树和多租户；
2. 邮件/短信通知；
3. AI 导出到聊天的 `catscoArtifactHost` 通道；
4. 高级报表、图表和审批配置；
5. 对象存储、CDN 和大文件分片上传。

### 主要风险

- **大规模重构风险**：先复用现有 API 和组件，按页面逐步拆分，避免一次性重写 `orderDeskState.svelte.ts`；
- **状态迁移风险**：使用稳定枚举 code，保留显示映射和旧字段兼容，不直接替换历史中文值；
- **权限遗漏风险**：所有 API 先执行认证，再执行记录级权限检查，附件和导出不能例外；
- **金额不一致风险**：数据库统一以分为准，服务端重新计算导出金额，前端计算只做预览；
- **原型与正式产品混淆**：demo3 只提供用户可见行为和 UI 参考，不能把内存 seed、前端角色切换和伪下载作为正式实现。

---

## 9. 交付顺序建议

建议按以下可独立验收的增量提交开发：

1. `feat: add session auth and role guards`
2. `refactor: split workbench routes and design tokens`
3. `feat: align order list and entry workflow with demo3`
4. `feat: persist customer quote and vendor cost sources`
5. `feat: complete reimbursement review and payment workflow`
6. `feat: add permission-aware attachment and export services`
7. `test: cover auth workflow and business state transitions`
8. `docs: add deployment, migration and backup guide`

每个增量完成后都应至少执行：

```bash
npm run check
npm run build
```

并用真实 SQLite 数据完成对应阶段的手工验收。最终目标不是复制 `pics/demo3.html`，而是让现有全栈项目在数据持久化、身份权限、业务闭环和可维护性上承接 demo3 的产品体验。
